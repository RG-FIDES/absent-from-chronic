# Main requimetns:

You need to check files 2-ellis.R and 1-ferry.r and remove unecessary logs, must be only the unit test logs which notify that the scirpt have been run or not

The same with the flor.r, it must gives the review in the end which scirpts and which files have been run like a unit test. 

**The main idea**: the running of the flow.r must be as clean as possible, but not igrnore the availibility of runnign each ellis piepleine separetrly. 


Create a task to run each stage from the #manipulation like 1-ferry.r and 2-ellis.r. Also add a task to run each eda pipeline from the #analysis. It must run the r and qmd fiels. Check if there are already this scripts, if not add it. 


