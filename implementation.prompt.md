# Fixing the ps1 scripts:

Fix that if  I choose the "None" in the question with eda, it will check and remove any rows which must run any eda pipeline. Scan and check if it works correct. 


# Updating the text files with relevent infromation:

You need to update the next files with the relevent infromation about the ellis pipeline and the data stored in the `data-private/`:

- manipulation\pipeline.md (with the relevent state of the ellis pipeline, describe the inputs and outputs, and the way to run the pipeline, add detaild information how works the manipulation folder and the scripts in it)
- README.md(udpate with the information about repositroy and project, do not change much in the readme, just update with the current state of the project, tell the reader information where he can find the data, describe what we are doing in the manipulation folder, analysis, just add this information if it wont existed in it yet, do not change the structure of the readme, just add the information about the current state of the project and where to find the data and how to run the scripts in manipulation and analysis folders)
- manipulation\README.md (update with the information about the manipulation folder, describe the scripts in it, how to run them, what they do, and what data they produce, add the information about the ellis pipeline and how it works, what data it uses and produces, and how to run it)
- analysis\README.md (update with the information about the analysis folder, describe the scripts in it, how to run them, what they do, and what data they produce, add the information about the eda pipelines and how they work, what data they use and produce, and how to run them)